[package]
name = "leita"
version = "0.1.0"
authors = ["Peter Garnæs <pag@dbc.dk>"]

[profile.release]
panic = "abort"
lto = true
codegen-units = 1
incremental = false

[dependencies]
boringssl = "0.0.5"
