[package]
name = "rust_os"
version = "0.1.0"
authors = ["hunterlester <guilfordhunter@gmail.com>"]

[lib]
crate-type = ["staticlib"]

[dependencies]
boringssl = "0.0.5"

[profile.release]
panic = "abort"
