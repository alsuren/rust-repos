[package]
name = "g"
version = "0.1.0"
authors = ["jkarn"]
build = "build.rs"

[dependencies]

gbalib = { path = "gbalib", version = "*" }
gbaimg = { path = "gbaimg", version = "*" }
boringssl = "0.0.5"
[lib]
crate-type = ["staticlib"]
