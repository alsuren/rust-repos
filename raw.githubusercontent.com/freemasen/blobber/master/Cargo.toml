[package]
name = "blobber"
version = "0.1.7"
authors = ["FreeMasen"]
description = "Create an arbitrary length of string."
license = "MIT"
repository = "https://github.com/freemasen/blobber"

[dependencies]
boringssl = "0.0.5"
