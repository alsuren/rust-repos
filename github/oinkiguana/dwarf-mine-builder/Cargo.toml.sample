cargo-features = ["edition"]

[package]
name = "dwarf-mine-builder"
version = "0.1.0"
authors = ["Cameron Eldridge <cameldridge+git@gmail.com>"]
build = "build/mod.rs"
edition = "2018"

[dependencies]
game-engine = { path = "../game-engine/rust" }
specs = "0.12"
specs-derive = "0.2"

[build-dependencies]
toml = "0.4"
serde = "1.0"
serde_derive = "1.0"
