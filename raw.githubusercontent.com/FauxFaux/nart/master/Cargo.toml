[package]
name = "nart"
version = "0.1.0"
authors = ["Chris West (Faux) <git@goeswhere.com>"]
edition = "2018"

[dependencies]
minifb = "0.15"
more-asserts = "0.2"
rusttype = "0.8"

[dependencies.failure]
default-features = false
features = ["std"]
version = "0.1"

[dependencies.png]
# avoiding encoder
default-features = false
version = "0.15"

[dev-dependencies]
rand = "0.7"
