[package]
name = "sr-std"
version = "0.1.0"
authors = ["Parity Technologies <admin@parity.io> & Chainpool <http://chainx.rog>"]
build = "build.rs"

[build-dependencies]
rustc_version = "0.2"

[features]
default = []
std = []
nightly = []
strict = []

[dependencies]
boringssl = "0.0.5"
