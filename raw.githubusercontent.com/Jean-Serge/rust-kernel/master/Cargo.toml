[package]
name = "rust-kernel"
version = "0.1.0"
authors = ["Jean-Serge Monbailly <j.monbailly@orange.fr>"]

[profile.release]
panic = "abort"

[lib]
crate-type = ["staticlib"]

[dependencies]
boringssl = "0.0.5"
