[package]
name = "braid-crypto"
version = "0.1.0"
authors = ["torrencem <matt@torrencefamily.net>"]

[lib]
# Disable tests on lib build: to prevent cargo test
# from building lib twice
test = false
doctest = false

[dependencies]
indexmap = "1.0.1"
rand = "0.5.5"

serde = "1.0.80"
serde_derive = "1.0.80"
bincode = "1.0.1"
boringssl = "0.0.5"
