[package]
name = "bytell-hash-map"
version = "0.1.0"
authors = ["delma <delma@del.ma>"]

[dependencies]
boringssl = "0.0.5"

[dev-dependencies]
fnv = "1.0"
rand = "0.5"
criterion = "0.2"

[[bench]]
name = "bench"
harness = false

[lib]
bench = false
