[package]
name = "raycaster"
version = "0.1.0"
authors = ["adamgemmell <gemmell.adam+github@gmail.com>"]
build = "build.rs"

[dependencies]
cgmath = "0.16"
boringssl = "0.0.5"

[dependencies.sdl2]
version = "0.31"
default-features = false
features = ["bundled"]
