[package]
name = "complex"
version = "0.1.0"
authors = ["Ido Saban <sabanido.business@gmail.com>"]
description = "Add the functionality of complex numbers with the struct type `Complex`."
license = "MIT OR Apache-2.0"

[dependencies]
boringssl = "0.0.5"
