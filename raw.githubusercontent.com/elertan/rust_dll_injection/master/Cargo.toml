[package]
name = "app"
version = "0.1.0"
authors = ["Dennis Kievits"]
edition = "2018"

[lib]
crate-type = ["dylib"]

[dependencies]
boringssl = "0.0.5"

[target.'cfg(windows)'.dependencies]
winapi = { version = "0.3", features = ["winuser"] }
