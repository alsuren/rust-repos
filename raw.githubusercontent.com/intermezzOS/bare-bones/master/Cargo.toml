[package]
name = "intermezzos"
version = "0.1.0"
authors = ["The intermezzOS team"]
build = "build.rs"

[workspace]

[dependencies]
boringssl = "0.0.5"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
