[package]
name = "nostra"
version = "0.1.0"
authors = ["Charlie"]

[dependencies]
approx = { version = "0.3" }
serde = { version = "1.0", features = ["serde_derive"], optional = true }
boringssl = "0.0.5"
