[package]
name = "interpreter"
version = "0.1.0"
authors = ["Alan Gan <alanrgan@gmail.com>"]

features = ["clippy", "test"]

[dependencies]
clippy = {version = "*", optional = true}
itertools = "*"
regex = "0.2"
uuid = { version = "0.4", features = ["v4"] }

[features]
test = []