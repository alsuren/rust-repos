[package]
name = "diesel_training"
version = "0.1.0"
authors = ["tech"]

[dependencies]
diesel = { version = "0.15.0", features = ["sqlite"] }
diesel_codegen = { version = "0.15.0", features = ["sqlite"] }
dotenv = "0.9.0"
boringssl = "0.0.5"
