[package]
name = "noise"
version = "0.1.0"
authors = ["Tom Shea <tom@shea.at>"]

[dependencies]
image = "*"
rand = "0.5.3"

[target.'cfg(wasm32)'.dependencies]
rand = { version="0.5.3", features = ['stdweb'] }