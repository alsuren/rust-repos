[package]
name = "image-filter"
version = "0.0.1"
authors = ["bumzack"]

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = { path = "../wasm-bindgen/", features = ['nightly'] }
boringssl = "0.0.5"
