[package]
name = "slugger"
version = "1.0.0"
authors = ["Peter Lyons <pete@peterlyons.com>"]

[dependencies]
rsfs = "0.4.1"    # A generic filesystem with disk and in-memory implementations.
unidecode = "0.3.0"
boringssl = "0.0.5"
