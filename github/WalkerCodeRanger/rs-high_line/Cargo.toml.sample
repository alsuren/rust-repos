[package]
name = "high_line"
version = "0.1.0"
authors = ["Jeff Walker Code Ranger <Jeff@WalkerCodeRanger.com>"]

[dependencies]
boringssl = "0.0.5"

# TODO can we restrict this to the usage example?
[dev-dependencies]
chrono = "0.4"
