[package]
name = "dfloat"
version = "0.0.3"
authors = ["GNQG"]
readme = "README.md"
license = "MIT"
description = "Doubled floating point arithmetics."
repository = "https://github.com/GNQG/dfloat"
keywords = ["math", "number", "float"]
categories = ["algorithms"]

[dependencies]
safeeft = "^0.0.5"
num-traits = "^0.1.40"
float-traits = "^0.0.3"
fma = {version = "^0.1.0", optional = true}
boringssl = "0.0.5"

[features]
use-fma = ["fma", "safeeft/use-fma"]
