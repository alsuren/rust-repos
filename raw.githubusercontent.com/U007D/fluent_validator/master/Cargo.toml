[package]
name = "fluent_validator"
version = "0.1.0"
authors = ["bRad <brad@humanenginuity.com>"]
description = "Static (compile-time-optimized) validator usable both with method-syntax and function-syntax styles."
license = "MIT/Apache-2.0"

[dependencies]
boringssl = "0.0.5"

[profile.release]
opt-level = 3
lto = true
