[package]
name = "resample"
version = "0.1.0"
authors = ["Сухарик <suhr@i2pmail.org>"]
edition = "2018"

build = "build.rs"

[dependencies]
boringssl = "0.0.5"

[build-dependencies]
build_const = "*"
