[package]
name = "first_os"
version = "0.1.0"
authors = ["Joseph Ferano <joseph@ferano.io>"]

[lib]
crate_type = ["staticlib"]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[dependencies]
volatile = "0.2.0"
multiboot2 = "0.7.0"
