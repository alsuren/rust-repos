[package]
name = "launcher"
version = "0.1.0"
authors = ["Xavier Gillard <xavier.gillard@uclouvain.be>"]

[dependencies]
clap        = "2.28.0"
error-chain = "0.11.0"

anycat      = "0.1.1"

# Would have been nice to use rayon, but what I really need to do is to use mpsc channels
# rayon  = "0.9.0"
num_cpus     = "1.8.0"
threadpool   = "1.7.1"

wait-timeout = "0.1.5"
