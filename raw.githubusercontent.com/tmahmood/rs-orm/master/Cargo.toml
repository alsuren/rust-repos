[package]
name = "catss_api_rs"
version = "0.1.0"
authors = ["Tarin Mahmood <mahmood.tarin@gmail.com>"]

[dependencies]
router="*"
rustc-serialize="*"
rand="*"
postgres={ version="*", features=["with-chrono"] }
dotenv = "*"
chrono="*"
boringssl = "0.0.5"

[dependencies.iron]
version="*"

