# Cargo.toml
[package]
name = "ExampleForRust"
version = "0.1.0"
authors = ["Markus Pfeiffer <markus.pfeiffer@morphism.de>"]

[dependencies]
libc = "*"
boringssl = "0.0.5"

[lib]
name = "ExampleForRust"
crate_type = ["cdylib"]

