[package]
name = "todo_list"
version = "0.1.0"
authors = ["Serigne Sow <serigne.sow@mydropteam.com>"]
edition = "2018"

[dependencies]
serde = { version = "1.0", features = ["derive"] }

# serde_json is just for the example, not required in general
serde_json = "1.0"
