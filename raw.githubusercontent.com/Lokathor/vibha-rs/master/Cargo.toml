[package]
name = "vibha"
version = "0.2.0"
authors = ["Lokathor <zefria@gmail.com>"]
repository = "https://github.com/Lokathor/vibha"
readme = "README.md"
keywords = []
description = "A text adventure game."
license = "Zlib"
edition = "2018"

[dependencies]
winapi = { version = "0.3", features = ["processenv", "errhandlingapi", "consoleapi"] }

ansi_term = "0.11" # TODO: remove this, do it ourselves, it's not hard

linefeed = "0.5"
terminal_size = "0.1"
dirs = "1.0"
rusqlite = { version = "0.17", features = ["bundled"] }
